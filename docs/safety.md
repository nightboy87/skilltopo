# Safety Notes

SkillTopo can reduce routing mistakes, but it cannot guarantee safe execution.

Recommended safety policy:

- `low`: allow normal recommendation;
- `medium`: show recommendation reason and matched terms;
- `high`: require explicit confirmation;
- `critical`: require explicit confirmation and external permission checks.

For external sending, deletion, approval, payment, credential access, and production changes, never rely on routing alone.
